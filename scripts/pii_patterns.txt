# PII pattern file consumed by full_pii_scan.sh and pre-commit-pii-scan.sh.
# One regex per line. Lines starting with # are ignored.
#
# Maintenance rule:
#   * Add a new line whenever a PII token is discovered in a sanitization
#     pass; never delete a line without a written justification.
#   * Do NOT add the GitHub username of the project owner here. The
#     owner's GH handle appears in the canonical repo URL and is public
#     by definition (CITATION.cff, README badges, install instructions).
#
# Keep this list synchronized with the staged-diff scanner.

# Maintainer real name + nicknames (Chinese)
胡英实
腊八粥

# Maintainer Unix username (must NOT match composite words like 'huyingshi_demo')
huyingshi[^_]
yingshihu[^_]

# Maintainer phone-pin / QQ id / iPhone unlock pin
2942468748
070319

# Maintainer institutional email + IPs
@mail\.ustc\.edu\.cn
@ustc\.edu\.cn
biexin839967
100\.73\.32\.96
222\.195\.68\.89

# Real institutional / personal portals that previously leaked into samples
bb\.ustc\.edu\.cn
pems\.ustc\.edu\.cn
新荷学者
ruiyin
